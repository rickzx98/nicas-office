import { LABEL_CONFIRM_PAGE_LEAVE_UNSAVED_CHANGES } from '../../labels/';
import React from 'react';

export const CancelModalBody = () => {
  return <p>{LABEL_CONFIRM_PAGE_LEAVE_UNSAVED_CHANGES}</p>;
};
